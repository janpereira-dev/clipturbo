# Demos

Este directorio guarda insumos livianos de demo (`.txt`, `.srt`) y evita versionar binarios pesados.

Reglas:

- no subir salidas de ejecucion (`.mp4`, `.wav`, `.mp3`)
- usar `media/generated/` para artefactos temporales
- conservar en git solo plantillas o ejemplos pequenos
